# archive/

Local-only leftovers. **Contents are gitignored** except this README
(and `duplicates/README.md`).

| Local file | Status |
|------------|--------|
| `raspberry login.txt` | Secret — never push |
| `set_wifi.sh` | Replaced by `host/attacks/prepare_wifi.sh` |
| `nids_collector_v2.py` | Unused |
| `duplicates/*` | Old `.py` attack twins — use `host/attacks/*.sh` |

Teammates do not need anything under `archive/` to run the project.
